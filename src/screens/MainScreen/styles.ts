import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {},
  todosContainer: {
    padding: 5,
  },
  image: {
    width: '100%',
    height: '90%',
    resizeMode: 'contain',
  },
});

export default styles;
